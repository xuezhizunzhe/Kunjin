interface:
  display_name: "KunJin Fund Research"
  short_description: "Route personal funds with classification evidence"
  default_prompt: "Use $kunjin-fund to route each fund subquestion by action, return evidence-backed research_only or conditional guidance, and never trade automatically."
